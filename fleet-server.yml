outputs:
  default:
    type: elasticsearch
    hosts:
      - https://es01:9200
    username: elastic
    password: changeme_elastic
    ssl:
      certificate_authorities:
        - /usr/share/fleet-server/certs/ca/ca.crt
