outputs:
  default:
    type: elasticsearch
    hosts:
      - https://es01:9200
    username: elastic
    password: ${ELASTIC_PASSWORD}
    ssl:
      certificate_authorities:
        - /usr/share/fleet-server/certs/ca/ca.crt
